<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/stylesheets/style.css">
    <title>Edit Comic</title>
</head>

<body>
    <%- include('../partials/_navbar.ejs') %>

    <h1>Edit this comic entry</h1>

    <form action="/users/<%= user._id %>/comic-list/<%= comic._id %>?_method=PUT" method="POST" class="standard-list">
        <label for="name">Name:</label>
        <input type="text" name="name" id="name" value="<%= comic.name %>">
        <br>
        <label for="status">Status:</label>
        <select id="status" name="status">
            <option value="plan-to-read" <%= comic.status === 'plan-to-read' ? 'selected' : '' %>>
                Plan to Read
            </option>
            <option value="reading" <%= comic.status === 'reading' ? 'selected' : '' %>>
                Reading
            </option>
            <option value="completed" <%= comic.status === 'completed' ? 'selected' : '' %>>
                Completed
            </option>
            <option value="dropped" <%= comic.status === 'dropped' && 'selected' %>>
                Dropped
            </option>
        </select>
        <br>
        <br>
        <button type="submit">Update Application</button>
    </form>
</body>

</html>